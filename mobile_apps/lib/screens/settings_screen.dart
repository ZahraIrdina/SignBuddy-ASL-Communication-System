import 'package:flutter/material.dart';
class SettingsScreen extends StatefulWidget{const SettingsScreen({super.key});@override State<SettingsScreen> createState()=>_S();}
class _S extends State<SettingsScreen>{bool dark=false,auto=true;@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Settings')),body:ListView(padding:const EdgeInsets.all(18),children:[const Text('General',style:TextStyle(fontWeight:FontWeight.bold)),ListTile(leading:const Icon(Icons.volume_up),title:const Text('Voice Speed'),trailing:const Text('Normal >')),ListTile(leading:const Icon(Icons.text_fields),title:const Text('Text Size'),trailing:const Text('Medium >')),SwitchListTile(title:const Text('Dark Mode'),value:dark,onChanged:(v)=>setState(()=>dark=v)),const Text('Playback',style:TextStyle(fontWeight:FontWeight.bold)),SwitchListTile(title:const Text('Auto Play'),value:auto,onChanged:(v)=>setState(()=>auto=v)),const Text('Storage (Local)',style:TextStyle(fontWeight:FontWeight.bold)),const ListTile(leading:Icon(Icons.delete_outline),title:Text('Clear History')),const ListTile(leading:Icon(Icons.delete_sweep),title:Text('Clear Favorites')),const SizedBox(height:35),const Center(child:Text('All data stored locally on your device'))]));}
